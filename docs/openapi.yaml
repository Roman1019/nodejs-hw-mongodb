openapi: 3.1.0
info:
  version: 1.0.0
  title: Contacts book
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: This is a documentation of contacts book
tags:
  - name: Contacts
    description: Operations with contacts.
servers:
  - url: http://localhost:3000
  - url: https://nodejs-hw-mongodb-1-of8w.onrender.com
paths:
  /contacts/{id}:
    get:
      $ref: '../swagger/path/contacts/by-id/get.yaml'
    patch:
      $ref: '../swagger/path/contacts/by-id/patch.yaml'
    delete:
      $ref: '../swagger/path/contacts/by-id/delete.yaml'
  /contacts:
    get:
      $ref: '../swagger/path/contacts/get.yaml'
    post:
      $ref: '../swagger/path/contacts/post.yaml'

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
